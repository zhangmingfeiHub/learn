<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:rabbit="http://www.springframework.org/schema/rabbit"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
	http://www.springframework.org/schema/rabbit http://www.springframework.org/schema/rabbit/spring-rabbit.xsd ">

	<rabbit:connection-factory id="factory" 
				host="${rabbitmq.connection.host}" 
				username="${rabbitmq.connection.username}" 
				password="${rabbitmq.connection.password}" 
				virtual-host="${rabbitmq.connection.vhost}"
				port="${rabbitmq.connection.port}" />
				
	<rabbit:admin id="admin" connection-factory="factory" />
	
	<rabbit:queue name="${rabbitmq.connection.queuename.my-queue-spring}"></rabbit:queue>
	
	<rabbit:fanout-exchange name="myExchangeFanout" >
		<rabbit:bindings>
			<rabbit:binding queue="${rabbitmq.connection.queuename.my-queue-spring}"></rabbit:binding>
		</rabbit:bindings>
	</rabbit:fanout-exchange>

	<rabbit:listener-container connection-factory="factory">
		<rabbit:listener ref="recvMessage" queue-names="${rabbitmq.connection.queuename.my-queue-spring}" method="recv"/>
	</rabbit:listener-container>
	
	<rabbit:template id="rabbitTemplate" connection-factory="factory" exchange="myExchangeFanout"></rabbit:template>

</beans>
